---
import LocalizedHome from '../../components/LocalizedHome.astro';

// Localised homepage. Content lives in src/i18n/home.ts so this file stays a
// one-liner and translations are reviewable in one place.
---

<LocalizedHome locale="de" />
