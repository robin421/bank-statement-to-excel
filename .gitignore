node_modules/
dist/
.astro/
.env
.env.local
.DS_Store
._*

# NEVER commit real bank statements (PII). Local grading only.
fixtures/real/
*.local.pdf
.vercel

# Derived verification reports (aggregate-only, but local artifacts)
fixtures/reports/

# Prototype scratch
/fixtures/reports/

# Corpus candidates and text dumps: re-downloadable, ~32MB, and statements are
# third-party documents. Only the manifest and the report are committed.
/corpus/candidates/
/corpus/dump/
